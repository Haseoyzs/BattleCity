package battlecity.util

internal object Config {
    // 战场最大敌人数量
    internal var maxCount = 4
    // 敌方坦克总数
    internal var enemySum = 20
    // 默认开始关卡
    internal var stageIndex = 1
    // 默认临界分数
    internal var hiScore = 20000L
}